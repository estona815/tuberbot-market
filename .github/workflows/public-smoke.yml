name: Historical review URL smoke
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  note:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Use the acquisition public verification for the v3 site; the v2 immutable public smoke is archived.'
